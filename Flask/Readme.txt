In this folder is the code for the flask app. 
Flask uses the html code stored in /templates to display based on the python code.
The fileupload.py runs on port 33.
The fileupload88.py runs on port 88.
The reason for two of them is for testing.
##TO RUN##
pip install flask
pyrhon3 fileupload.py
Open web browser and go to http://YOUR_IP:33

You need to have the templates folder in the same ditrectory as the python code
